# تقرير الإصلاحات — Bookshop (Block Book)

تاريخ التنفيذ: **17 أغسطس 2026**
النطاق: `backend/` (Express + Prisma) و `frontend/` (React + CRA + TypeScript)

---

## ⚠️ مطلوب منك يدوياً — افعل هذا أولاً

ملف `backend/.env` كان **مرفوعاً على git** بكل المفاتيح الحقيقية. أزلته من التتبّع وأضفت `.gitignore` صحيحاً، لكن **المفاتيح ما زالت موجودة في تاريخ git** ويجب اعتبارها مكشوفة:

| المفتاح | الإجراء المطلوب | أين |
|---|---|---|
| `DATABASE_URL` | غيّر كلمة مرور قاعدة البيانات | لوحة تحكم مزوّد Postgres |
| `STRIPE_SECRET_KEY` | Roll key | Stripe Dashboard → Developers → API keys |
| `CLOUD_API_SECRET` | Regenerate | Cloudinary → Settings → Security |
| `JWT_SECRET` | ✅ **تم تدويره تلقائياً** (96 حرفاً عشوائياً) | — |

> تدوير `JWT_SECRET` يعني أن كل الجلسات القديمة أصبحت غير صالحة — كل المستخدمين سيحتاجون تسجيل دخول جديد. هذا مقصود لأن السر القديم كان مكشوفاً.

لتنظيف تاريخ git من الملف (اختياري لكن مُوصى به بشدة):

```bash
# باستخدام git-filter-repo (الأفضل)
pip install git-filter-repo
git filter-repo --path backend/.env --invert-paths --force
git push --force
```

كذلك أضف نفس المتغيّرات في لوحة تحكم الاستضافة (Render / Vercel) — راجع `backend/.env.example`.

---

## 1) الإصلاحات الأمنية

### 1.1 تسريب هاشات كلمات المرور
كانت أربع نقاط تُرجع سجل المستخدم كاملاً بما فيه عمود `password`:

| النقطة | قبل | بعد |
|---|---|---|
| `POST /api/auth/login` | يُرجع `user` كاملاً → يُخزَّن في `localStorage` | `select` صريح بلا `password` |
| `GET /api/users/:id` | السجل كاملاً | `publicUserSelect` (والإيميل لصاحب الحساب فقط) |
| `GET /api/books/:id/comments` | `include: { user: true }` | `select` صريح |
| `GET /api/messages/:id` | `include: { sender: true }` | `select` صريح |

الحل المركزي: ملف جديد `backend/utils/selects.js` فيه `publicUserSelect` و `selfUserSelect` و `bookSelect`. **القاعدة: لا `include: { user: true }` أبداً.**

### 1.2 JWT بلا انتهاء صلاحية
`jwt.sign()` كان بلا `expiresIn` → توكن أبدي غير قابل للإبطال.
الآن: `expiresIn` من `JWT_EXPIRES_IN` (افتراضي `7d`)، و `verifyToken` يميّز بين "منتهي" و"غير صالح" ويرد `401` في الحالتين (كان `403` يربك الواجهة).

### 1.3 تجاوز البوابة المدفوعة (كان أخطر خلل وظيفي)
كان `has_paid` يُقرأ من `localStorage` فقط → تعديل قيمة واحدة في devtools يفتح كل المزايا المدفوعة، والـ API لم يكن يفحص شيئاً إطلاقاً.

الآن:
- **الخادم** هو المرجع: `requireBookAccess` و `requireChapterAccess` في `backend/middlewares/authorize.js` — القاعدة: *مالك الكتاب أو مشترك فعّال*.
- تُطبَّق على: إنشاء فصل، كتابة محتوى فصل، قراءة فصل، قراءة محتوى فصل.
- **الواجهة** تسأل `GET /api/users/me` عبر hook جديد `useAccount()` بدل قراءة `localStorage`.
- **تسريب إضافي اكتُشف أثناء العمل:** `GET /api/books/:id/chapters` كان يُرجع `chapter_content` (النص والصوت كاملاً) لأي مستخدم مسجّل — أي أن المحتوى المدفوع كان مكشوفاً عبر قائمة الفصول. الآن القائمة تُرجع الغلاف والعنوان فقط (`chapterListSelect`).

### 1.4 غياب التحقق من الملكية (IDOR)

| العملية | قبل | بعد |
|---|---|---|
| حذف تعليق | أي مستخدم يحذف أي تعليق | صاحب التعليق أو مالك الكتاب فقط |
| إنشاء فصل | أي مستخدم في أي كتاب | مالك أو مشترك |
| كتابة محتوى فصل | أي مستخدم | مالك أو مشترك |
| قراءة رسائل محادثة | أي مستخدم بمعرفة الرقم | المشاركون فقط |
| إرسال رسالة | أي مستخدم لأي محادثة | المشاركون فقط |
| `POST /api/conversations` | أي مستخدم ينشئ محادثة بأي مشاركين | **حُذفت** (غير مستخدمة من الواجهة وكانت ثغرة) |

### 1.5 رفع الملفات كان مفتوحاً للعالم
`/api/upload/image` و `/api/upload/audio` بلا `verifyToken` وبلا حد حجم → أي شخص يستنزف حساب Cloudinary.
الآن: `verifyToken` + حد 5MB للصور و20MB للصوت + `fileFilter` على نوع MIME + rate limit (30/ساعة) + رد `503` واضح إذا كان Cloudinary غير مُعدّ.

### 1.6 كشف وجود المستخدمين (User Enumeration)
كان `404 "User not found"` مقابل `401 "Wrong Password"`.
الآن: رسالة واحدة `401 "Incorrect email or password."` — مع `bcrypt.compare` على هاش وهمي في حالة الإيميل غير الموجود حتى **زمن الرد** لا يكشف الفرق أيضاً.

### 1.7 التحقق من الدفع
- `GET /api/payment/success` كان **بلا مصادقة** ويقبل أي `session_id`. الآن: يتطلب توكن + يتحقق أن `client_reference_id` في جلسة Stripe يساوي هوية المستخدم الحالي.
- **جديد:** `POST /api/payment/webhook` بتوقيع Stripe موثّق (`STRIPE_WEBHOOK_SECRET`) — هذا هو المصدر الموثوق للدفع حتى لو أغلق المستخدم المتصفح قبل العودة. مُثبَّت بـ `express.raw` قبل `express.json` (شرط صحة التوقيع)، ويرد `503` إن لم يُضبط السر.
- `success_url` / `cancel_url` كانا يُبنيان من `req.headers.origin` و `req.headers.referer` (قابلة للتزوير وقد تكون `undefined` → `"undefined/success"`). الآن من قائمة `CLIENT_URLS` الموثوقة.

### 1.8 تحصينات عامة أُضيفت
- `helmet()` لرؤوس الأمان.
- Rate limiting: 20 محاولة دخول/15 دقيقة، 30 رفع/ساعة، 300 طلب/دقيقة عامة.
- `express.json({ limit: "1mb" })`.
- `validateRequest` صار يستبدل `req.body` بالمُخرَج المُتحقَّق منه → **حقول زائدة مثل `has_paid` تُحذف تلقائياً** ولا تصل إلى Prisma.
- `app.set("trust proxy", 1)` ليعمل الـ rate limiting خلف Render/Vercel.

---

## 2) إصلاحات التشغيل والنشر

### 2.1 `dotenv.config()` كان يعمل بعد الـ imports
في ESM كل الـ imports تُنفَّذ قبل جسم الملف، فـ `new Stripe(process.env.STRIPE_SECRET_KEY)` و `cloudinary.config()` كانا يقرآن قيماً فارغة.
**تحققت عملياً:** المشروع كان يعمل *بالصدفة* فقط لأن `@prisma/client` يحمّل `.env` كأثر جانبي عند استيراده قبلهما — أي تغيير في ترتيب الاستيراد كان سيكسر الدفع والرفع صامتاً.

الحل: ملف جديد `backend/utils/env.js` يستورد `dotenv/config` ويُستورد كأول سطر في `server.js`، ويتحقق من المتغيّرات المطلوبة ويوقف التشغيل برسالة واضحة إن نقصت.

### 2.2 CORS
كان مكتوباً بشكل ثابت (`localhost:3000` في نسختك المحلية، ورابط Vercel في نسخة git) — أي أن الإنتاج كان مكسوراً في إحدى الحالتين دائماً.
الآن: قائمة من `CLIENT_URLS` مفصولة بفواصل، مع رد `403` مفهوم عند رفض الأصل.

### 2.3 المنفذ والنشر
- `app.listen(5000)` ← الآن `process.env.PORT` (شرط عمل Render).
- `vercel.json` كان يوجّه إلى `/index.js` وهو ملف غير موجود ← صار `server.js`.
- `npm start` كان يشغّل `nodemon` (أداة تطوير) في الإنتاج ← الآن `start: node server.js` و `dev: nodemon server.js`.

### 2.4 اتصال قاعدة البيانات
`utils/db.js` كان ينفّذ `$disconnect()` في `finally` مباشرة بعد `$connect()`. الآن: يبقى الاتصال مفتوحاً، مع `process.exit(1)` عند فشل الاتصال و `$disconnect()` نظيف عند `SIGINT`/`SIGTERM`.

### 2.5 معالجة الأخطاء
`errorHandler` كان مسجّلاً لكن لا يصله شيء (كل الأخطاء ملتقطة محلياً)، وكان يرد نصاً خاماً.
الآن:
- `asyncHandler` يلفّ كل متحكم فتصل أخطاء `async` للمعالج المركزي.
- ترجمة أخطاء Prisma: `P2002 → 409`، `P2025 → 404`، `P2003 → 400`، وأخطاء التحقق → `400`.
- أخطاء Multer (حجم/نوع الملف) → `400` برسالة مفهومة.
- في الإنتاج لا تُسرَّب تفاصيل أخطاء 500.
- `notFoundHandler` لأي مسار API غير معروف بدل رد HTML غامض.
- `parseId()` يرفض المعرّفات غير الرقمية بـ `400` بدل تمرير `NaN` إلى Prisma (كان يعطي `500`).

### 2.6 تنظيف الاعتماديات
- **backend:** حُذفت `body-parser` (كانت مكررة مع `express.json`)، و `@types/*` غير اللازمة في مشروع JS، ونُقلت `nodemon` إلى `devDependencies`. أُضيفت `helmet` و `express-rate-limit`.
- **frontend:** حُذفت `stripe` (SDK الخادم! كان يُحزم في المتصفح)، `react-query` v3 (مكررة مع `@tanstack/react-query` v5)، و `formik`، `swiper`، `jwt-decode`، `react-spinners`، `@types/stripe-v3` — كلها غير مستخدمة إطلاقاً.

---

## 3) إصلاحات الواجهة الأمامية

### 3.1 التوست (كما طلبت)
**المشكلة الجذرية:** `ToasterProvider` كان داخل `Layout` المحمي فقط — أي أن **كل توست في صفحات تسجيل الدخول والتسجيل والاستبيان لم يكن يظهر إطلاقاً**. نُقل إلى جذر `App.tsx` بجانب `RouterProvider`.

توست أُضيف حيث لم يكن موجوداً:
- فشل المتابعة/إلغاء المتابعة والإعجاب (كانت الأخطاء تُبتلع صامتة تماماً).
- فشل تحميل التعليقات/إرسالها/حذفها.
- إنشاء الكتاب، رفع الغلاف، إنشاء الفصل، رفع المحتوى.
- إرسال الرسائل.
- انتهاء الجلسة (401) → توست + تسجيل خروج تلقائي + تحويل لصفحة الدخول.
- تسجيل الخروج، والتحقق من الاشتراك، وفشل تشغيل الصوت.
- رسائل تحقق قبل الإرسال: "اكتب نصاً أولاً"، "ارفع غلافاً"، "اختر تخصصاً"، "اختر تصنيفاً واحداً على الأقل".

كما أُصلح **نمط خاطئ**: استدعاء `toast()` داخل جسم الـ render في `PaymentRoute` (يسبب حلقة عند إعادة الرسم) → نُقل إلى `useEffect`.

### 3.2 انهيارات فعلية أُصلحت

| المكان | السبب | الإصلاح |
|---|---|---|
| `MessageHeader.tsx` | `book.id` بلا `?.` والبيانات تأتي من `location.state` الذي يضيع عند التحديث | البيانات تُجلب الآن من `GET /api/conversations/:id` وتُمرَّر كـ prop |
| `Chapter.tsx` | `bookId` من `location.state` → صفحة فارغة عند التحديث أو الرابط المباشر | نقطة API جديدة `GET /api/chapters/:id` تُغني عن الحاجة لـ `bookId` |
| `Discover.tsx` | الخادم يُرجع كائناً بدل مصفوفة عند وجود أقل من 3 كتب → `books.map is not a function` | الخادم يُرجع مصفوفة دائماً + حارس `Array.isArray` + رسالة "لا توجد كتب بعد" |
| `Messages.tsx` / `Conversation.tsx` | `book.main_cover` بلا حارس والمحادثة قد تكون بلا كتاب | حراسة كاملة + صورة افتراضية |
| `helpers.getUser()` | `JSON.parse(...!)` ينهار إذا كان التخزين تالفاً أو فارغاً | `utils/session.ts` جديد: قراءة آمنة تُعيد `null` وتنظّف التالف |
| أي رابط خاطئ | لا مسار catch-all → صفحة بيضاء | صفحة `NotFound` + مسار `*` |

### 3.3 أخطاء منطقية

- **`Profile.tsx`:** فتح بروفايل شخص آخر كان يعرض **اسمه مع كتبك أنت** (كل الاستعلامات كانت مربوطة بـ `user.id` الخاص بك). الآن كل شيء يستخدم `profileId`.
- **وقت الرسالة:** كان `msg.sender.created_at` — أي **تاريخ إنشاء حساب المرسل** لكل الرسائل. الآن `msg.createdAt`.
- **"تاريخ الإنشاء":** كان `toLocaleTimeString` (يعرض ساعة تحت عنوان "تاريخ"). أُضيفت دوال `formatDate` / `formatClock` / `formatDateTime` في `helpers.ts` وتُستخدم في كل مكان.
- **ترتيب التعليقات:** القائمة تنازلية (الأحدث أولاً) لكن التعليق الجديد كان يُضاف في الآخر. الآن يُضاف في الأعلى.
- **زر Cancel** في صفحة إنشاء كتاب: كان يستخدم `redirect()` من react-router (تعمل داخل loaders/actions فقط) → لم يكن يفعل شيئاً. الآن `navigate()`.
- **تبويبات النافذة المنبثقة:** `TabButton` كان **بلا `onClick` إطلاقاً** → التبديل بين visual/audio/text مستحيل. الآن تعمل، مع تعطيل ذكي (إنشاء فصل من صفحة الكتاب فقط، إضافة محتوى من صفحة الفصل فقط) ورسالة توست توضّح السبب.
- **`window.location.reload()`** بعد رفع المحتوى → استُبدل بـ `invalidateQueries`.
- **أيقونات BookCard مقلوبة:** القلب كان يعرض عدد المتابعين والأشخاص يعرضون الإعجابات. صُحّحت.
- **زر الاشتراك في `SideBar`:** كان يقول "Create Content for 5$" للمشترك أصلاً. الآن: "Create New Chapter" للمشترك، و"Subscribe for $5 to contribute" لغيره.
- **`Success.tsx`:** كان يعدّل `localStorage` داخل جسم الـ render، وزر "Go to Previous Page" يستخدم `document.referrer` الذي يكون **صفحة Stripe** بعد التحويل (يعيدك لبوابة الدفع). الآن `useEffect` + `navigate("/tree")` + حالة فشل واضحة.
- **`Welcome.tsx`:** رابط إلى `/collection` وهو مسار غير موجود → `/profile`. وكان `<Button><Link/></Button>` (رابط داخل زر) → عُكس الترتيب.
- **`Questionnaire`:** بيانات التسجيل في zustand بلا حفظ → تحديث الصفحة يفقدها ويرسل طلباً ناقصاً. الآن: كشف الحالة وإعادة توجيه لـ `/register` مع توست. وبعد التسجيل يتم **تسجيل الدخول مباشرة** (الـ API صار يُرجع توكن) بدل مطالبة المستخدم بالدخول يدوياً.
- **حقول غير محكومة:** حقل الرسالة والتعليق كانا يُقرآن عبر `document.querySelector` و `previousElementSibling` → صارا `useState` محكومين، مع تعطيل زر الإرسال عند الفراغ وتمرير تلقائي لآخر رسالة.
- **`VoicePlayer`:** `removeEventListener` كان يُمرَّر دوالاً فارغة جديدة (تسريب مستمع عند كل mount)، ولا يوجد معالج `ended` (يبقى زر الإيقاف بعد انتهاء الصوت). أُصلح بالكامل + معالج `error` + `aria-label`.
- **`Modal`:** لم يكن فيه زر إغلاق مرئي إطلاقاً. أُضيف.
- **حد كلمة المرور:** الواجهة كانت تقبل أي طول والخادم كان يقبل نصاً فارغاً. الآن 8 أحرف على الجانبين.
- حُذفت كل `console.log` من كود الإنتاج، وأُصلح `;g:py-1` (خطأ طباعي في `Header`)، وأُضيفت `key` مفقودة و`alt`/`aria-label` للصور والأزرار.

### 3.4 معمارية الواجهة (ملفات جديدة)

| الملف | الغرض |
|---|---|
| `utils/session.ts` | القارئ/الكاتب الوحيد لجلسة `localStorage` — آمن ضد JSON التالف |
| `hooks/useAccount.ts` | حالة الحساب من الخادم (`has_paid` موثوق) |
| `hooks/useCheckout.ts` | منطق دفع Stripe موحّد (كان مكرراً في مكانين بسلوك مختلف) |
| `pages/NotFound.tsx` | صفحة 404 |
| `utils/newRequest.ts` | أُضيف `getErrorMessage()` + اعتراض 401 عام |

كما أُضيف إلى `newRequest` مفتاح `REACT_APP_API_URL` (كان الرابط مكتوباً بشكل ثابت داخل الكود)، ومفتاح Stripe العام انتقل إلى `REACT_APP_STRIPE_PUBLISHABLE_KEY` (كان مكتوباً حرفياً في `utils/stripe.ts`). القيم في `frontend/.env.local` (مُتجاهَل من git) والقالب في `frontend/.env.example`.

---

## 4) تغييرات في الـ API (مهمة إن كان لديك عملاء آخرون)

| النقطة | التغيير |
|---|---|
| `POST /api/auth/register` | كان يُرجع المستخدم فقط، الآن `{ token, user }` (دخول مباشر) |
| `POST /api/auth/login` | `user` بلا `password` |
| `GET /api/users/me` | **جديد** |
| `GET /api/chapters/:id` | **جديد** (فصل مستقل مع بيانات كتابه) |
| `GET /api/conversations/:id` | **جديد** |
| `GET /api/conversations/conversations/:id/messages` | حُذف المسار المكرر الخاطئ → `GET /api/conversations/:id/messages` |
| `POST /api/conversations` | **حُذف** (ثغرة، وغير مستخدم) |
| `GET /api/books/random-books` | يُرجع مصفوفة دائماً (كان كائناً عند < 3 كتب) |
| `GET /api/books/:id/chapters` | بلا `chapter_content` |
| `POST /api/payment/webhook` | **جديد** |
| كل الأخطاء | صيغة موحّدة `{ "error": "..." }` بدل نص خام |

---

## 5) ما تم اختباره فعلياً

**اختبار تكامل شامل (27 فحصاً — كلها ناجحة)** على قاعدة البيانات الحقيقية بحساب مؤقّت حُذف بالكامل بعد الاختبار (تم التحقق: 0 سجلات متبقية):

```
PASS  register returns 201 + token          PASS  create book -> 201
PASS  register response has no password     PASS  book creation also created a conversation
PASS  weak password rejected (400)          PASS  owner (unpaid) can create chapter -> 201
PASS  login returns 200 + token             PASS  owner can add text content -> 200
PASS  login response has no password hash   PASS  chapter list has no chapter_content
PASS  wrong password -> 401 generic         PASS  create/delete comment
PASS  GET /users/me works                   PASS  empty comment rejected -> 400
PASS  /users/me exposes has_paid=false      PASS  send/read message + createdAt
PASS  invalid book payload -> 400           PASS  non numeric id -> 400
...                                          27 passed, 0 failed
```

**اختبارات الصلاحيات (بمستخدمين حقيقيين من قاعدة البيانات):**

| الاختبار | النتيجة |
|---|---|
| قراءة فصل كمستخدم غير مشترك | `403` ✅ |
| قراءة فصل كمشترك | `200` ✅ |
| كتابة محتوى كغير مشترك | `403` ✅ |
| حذف تعليق شخص آخر | `403` ✅ |
| قراءة رسائل محادثة لست فيها | `403` ✅ |
| قراءة رسائل محادثة أنت فيها | `200` بلا حقل password ✅ |
| بروفايل مستخدم آخر | `200` بلا password/email ✅ |

**البناء:** `tsc --noEmit` نظيف، و `react-scripts build` ينجح بلا أي تحذير من كودنا (185 KB gzip)، وخادم التطوير يعمل على 3000 والـ backend على 5000.

> تحذير وحيد متبقٍ عند البناء: `Failed to parse source map ... tailwind-merge` — مشكلة تغليف في الحزمة نفسها (ملفات `.ts` مفقودة من التوزيعة)، لا علاقة لها بكودك ولا تؤثر على الإنتاج. لإخفائها: `GENERATE_SOURCEMAP=false` في `.env`.

---

## 6) التصميم المتجاوب (Responsive)

فُحص الموقع بالكامل بمتصفح حقيقي (Playwright/Chromium) على **4 مقاسات**: 320px، 375px، 768px، 1280px — أي **56 حالة** (14 صفحة × 4)، إضافة إلى النوافذ المنبثقة والقوائم المنسدلة.

### النتيجة النهائية

| المقاس | التمرير الأفقي | نصوص مقصوصة | صفحات بطول غير طبيعي |
|---|---|---|---|
| 320px | 0 | 0 | 0 |
| 375px | 0 | 0 | 0 |
| 768px | 0 | 0 | 0 |
| 1280px | 0 | 0 | 0 |

### أهم ما كان مكسوراً

**1. صفحة الشجرة `/tree` — كانت غير قابلة للاستخدام على الجوال**
الحلقات الخمس كانت موضوعة بـ `position: absolute` بنسب مئوية، وبعضها يُحسب من `getBoundingClientRect()` للصورة (`bottom: treeRect.width + 20px`) — على الجوال كانت تتكدّس فوق بعضها وفوق الشجرة.
الحل: أسفل `lg` صارت **شبكة أزرار واضحة** قابلة للنقر مع الصورة أعلاها؛ وفوق `lg` بقي تصميم الحلقات حول الشجرة كما هو (بمواضع ثابتة بالنِسَب بدل الحساب بالـ JS، فاختفت الحاجة لـ `useLayoutEffect` و`resize listener` بالكامل).
**إضافة:** نص الحلقات كان يعرض `&NBSP;` حرفياً لأن `react-curved-text` يستقبل نصاً خاماً لا HTML — استُبدلت الكيانات بمسافات غير قابلة للكسر حقيقية (` `).

**2. صفحة Discover — البطاقات متراكبة والتفاعل يعتمد على hover**
التصميم عبارة عن ثلاث بطاقات مروحية تتوسّع عند مرور الفأرة؛ على الجوال كانت تتراكب فوق بعضها بالكامل (الإزاحة `translate-x-6` أصغر من عرض البطاقة) و**اللمس لا يملك hover أصلاً** فلا طريقة لرؤيتها.
الحل: شبكة أغلفة عادية أسفل `lg`، والمروحة تبقى على الشاشات الكبيرة.

**3. النافذة المنبثقة `Modal` — دائرة على شاشة الجوال**
`rounded-full` + `h-[90vh]` + `justify-center` يعني أن الحواف الدائرية تأكل المحتوى ولا يمكن تمريره. الآن: `rounded-3xl` قابلة للتمرير أسفل `lg`، والدائرة الأصلية تبقى فوق `lg`. وأُضيف زر إغلاق مرئي (لم يكن موجوداً إطلاقاً).

**4. تبويبات إضافة المحتوى** كانت `px-6 text-xl` — ثلاثة تبويبات تتجاوز عرض 375px. صارت متدرّجة `px-2 sm:px-6` مع أيقونات أصغر ونص `truncate`.

**5. حقل إرسال الرسالة**: زر الإرسال كان `flex-1` فيأخذ **نصف عرض الشاشة**. صار `w-auto shrink-0`. وفقاعات الرسائل كانت `flex-1` (تمتد بعرض الصف كله) → صارت `max-w-[78%]`.

**6. `min-h-screen` متداخلة** في 4 أماكن (Messages، Questionnaire، SideImage، Success/ComingSoon) — كانت تجعل الصفحة بطول شاشتين ونصف مع فراغ ضخم. صفحة الرسائل على التابلت كانت طويلة لدرجة أن التقاط صورة كاملة لها فشل بمهلة 30 ثانية. الآن `flex-1` داخل حاوية `flex flex-col`.

**7. عناصر ثابتة العرض تتجاوز الشاشة**:
`w-96` لغلاف الكتاب، `w-[400px]` في صفحة Welcome وقائمة الإشعارات، `w-[300px]` لأزرار التواصل، `w-52` لبطاقات الفصول، `w-[250px]` لزر النافذة — كلها صارت `w-full max-w-…`.

**8. القائمة المنسدلة للملف الشخصي** كانت `-translate-x-[100px]` يدوياً فتخرج خارج الشاشة على الجوال → صارت `anchor="bottom end"` مع `max-w-[calc(100vw-1.5rem)]`.

**9. القرون الزخرفية في البروفايل** (`-left-10` / `-right-10` خارج الصندوق) كانت تدفع الصفحة أفقياً → تظهر من `sm` فأعلى فقط.

**10. النصوص الطويلة**: عناوين الكتب وأسماء المستخدمين لم تكن تُقصّ داخل الصفوف المرنة. أُضيف `min-w-0` + `truncate` (بدون `min-w-0` لا يعمل `truncate` داخل flex أصلاً).

### تحسينات عامة أُضيفت

- شبكة الكتب: `grid-cols-1 md:grid-cols-4` (قفزة من عمود واحد إلى أربعة) ← `1 → sm:2 → lg:3 → xl:4`.
- أغلفة الكتب صارت بنسبة `2/3` بدل مربّعة، فلم تعد أعلى الغلاف مقصوصة.
- أحجام خطوط متدرّجة للعناوين الكبيرة (`text-5xl sm:text-7xl lg:text-9xl` بدل `text-7xl` ثابت).
- حشوات متدرّجة بدل `p-12` / `px-24` الثابتة على الجوال.
- حماية عامة في `index.css`: `overflow-x: hidden` على `html/body`، و`max-width:100%` للصور، و`overflow-wrap: break-word` للنصوص الطويلة.
- `active:scale-95` على البطاقات القابلة للنقر (تغذية راجعة لمسية بديلة عن hover).

---

## 7) ما لم يُنفَّذ ومقترحات لاحقة

1. **تفعيل الـ webhook فعلياً:** الكود جاهز، ينقصك فقط إنشاء الـ endpoint في Stripe Dashboard ووضع `STRIPE_WEBHOOK_SECRET` في البيئة. بدونه يبقى الاعتماد على صفحة النجاح (آمنة الآن لكنها تفشل إن أغلق المستخدم التبويب).
2. **الرسائل لحظية:** التحديث حالياً بـ refetch عند الإرسال فقط. WebSocket/SSE أفضل لتجربة الدردشة.
3. **Rate limiting في الذاكرة:** يعمل لخادم واحد. مع أكثر من نسخة على Render ستحتاج مخزناً مشتركاً (Redis).
4. **"نسيت كلمة المرور"** و**تسجيل الدخول الاجتماعي**: الأزرار موجودة في الواجهة لكنها بلا وظيفة (`link: "#"`).
5. **جرس الإشعارات** يعرض رقم `2` ثابتاً ومعطّل حالياً في الهيدر — يحتاج نظاماً حقيقياً أو حذفاً.
6. **لا توجد اختبارات آلية في المشروع** — أدوات الاختبار مثبّتة لكن بلا أي ملف اختبار. أنصح بنقل سكربت التكامل الذي كتبته إلى `backend/tests/`.
7. **حذف الكتب/الفصول** غير موجود إطلاقاً في الـ API.
8. **فهارس قاعدة البيانات:** `user_books(book_id, type)` و `messages(conversationId, createdAt)` ستفيد الأداء عند النمو.

---

## 8) كيف تشغّل المشروع الآن

```bash
# backend
cd backend
cp .env.example .env      # ثم املأ القيم (أو استخدم .env الحالي بعد تدوير المفاتيح)
npm install
npm run dev               # nodemon على المنفذ من PORT (افتراضي 5000)

# frontend
cd frontend
cp .env.example .env.local
npm install
npm start                 # http://localhost:3000
```

للإنتاج: اضبط `CLIENT_URLS` في الـ backend على دومين الواجهة (مفصولة بفواصل إن كان أكثر من واحد)، و`REACT_APP_API_URL` في الواجهة على رابط الـ API.

---

# الجولة الثانية — الحذف وتحرير النص وتحسين الواجهة (18 أغسطس 2026)

## 8) إمكانية الحذف

لم يكن في الـ API أي طريقة لحذف المحتوى إطلاقاً. أُضيفت أربع عمليات، **كلها لمالك الكتاب فقط**، وكل واحدة تمرّ بنافذة تأكيد قبل التنفيذ:

| العملية | النقطة | ما يُحذف |
|---|---|---|
| حذف كتاب | `DELETE /api/books/:id` | الكتاب + فصوله + محتواها + تعليقاته + محادثته ورسائلها + علاقات المتابعة/الإعجاب — كلها في **معاملة واحدة** |
| حذف فصل | `DELETE /api/chapters/:chapterId` | الفصل ومحتواه (نص + صوت) |
| حذف فقرة | `DELETE /api/chapters/:chapterId/content/text/:index` | فقرة واحدة من داخل الفصل |
| حذف الصوت | `DELETE /api/chapters/:chapterId/content/audio` | مقطع الصوت مع إبقاء النص |

جدول قاعدة البيانات لا يحتوي `onDelete: Cascade`، لذلك حذف الكتاب يمرّ على كل جدول تابع بالترتيب الصحيح للمفاتيح الأجنبية داخل `$transaction` — إما أن ينجح كله أو لا يتغيّر شيء.

**لماذا المالك فقط؟** الفقرات مخزّنة كمصفوفة نصوص (`String[]`) بلا كاتب لكل فقرة، فلا توجد طريقة لمعرفة من كتب أي فقرة. السماح لأي مشترك بالحذف كان سيعني أن أي أحد يمسح كتابة غيره.

## 9) طريقة جديدة لإضافة النص

**قبل:** نافذة منبثقة → مربّع بحد 400 حرف → إرسال → تُضاف الفقرة في آخر المصفوفة. لا تعديل، لا حذف، ولا رؤية للنص أثناء الكتابة.

**بعد:** التحرير صار داخل صفحة الفصل نفسها:
- مربّع كتابة أسفل الفقرات مباشرة، يكبر تلقائياً مع النص (حتى 520px) بدل شقّ ثابت.
- **حد 2000 حرف** بدل 400، مع عدّاد أحرف.
- **Ctrl+Enter** للإضافة السريعة.
- زر تعديل ✎ وحذف 🗑 على كل فقرة (للمالك) — يظهران عند المرور بالفأرة على الشاشات الكبيرة، وبشكل دائم على اللمس.
- التعديل يفتح المحرّر مكان الفقرة نفسها، و`Esc` يلغيه.
- تبويب "text" أُزيل من النافذة المنبثقة لأنه لم يعد له داعٍ؛ بقيت النافذة لإنشاء الفصل ورفع الصوت فقط.

الفهرس يُتحقّق منه على الخادم: لو حذفت فقرة من تبويب وحاولت تعديل رقم قديم من تبويب آخر، يرد `400` برسالة "حدّث الصفحة" بدل الكتابة فوق فقرة خاطئة.

## 10) تحسين الواجهة والترتيب

### صفحة الكتاب — أُعيد بناؤها
كانت: شريط جانبي فيه زر دفع ثم صورة بمقاس ثابت `384×384` ثم الوصف، ومنطقة فصول فيها التصنيفات طائرة يميناً والتاريخ يعرض ساعة.

صارت: **غلاف + لوحة معلومات** جنباً إلى جنب، ثم **شبكة الفصول** تحتهما:
- العنوان والمؤلف → التصنيفات → الوصف → التاريخ وعدد الفصول → أزرار التفاعل → زر الإجراء الرئيسي.
- زر "Delete book" في أعلى اليمين للمالك.
- أزرار التعليقات/المتابعة/الإعجاب صارت شرائح بحجم محتواها بدل أزرار بعرض الصفحة كاملة.

### مشكلة انكماش الصور — سببها والحل
الغلاف كان `h-96 w-96` (مقاس ثابت) داخل عمود `flex`؛ وبطاقات الفصول `w-52` مع `<img className="h-full">` بلا ارتفاع محدّد للبطاقة. النتيجة: عندما يضيق العمود يضغط الفلكس الصورة، وارتفاع كل بطاقة يتبع صورتها فتظهر البطاقات بأطوال مختلفة.

الحل: كل غلاف صار داخل صندوق بنسبة ثابتة `aspect-[3/4]` مع `object-cover`، والشبكة `grid` بأعمدة تتغيّر حسب العرض (2 → 3 → 4 → 5). **تحققت آلياً**: كل الأغلفة تُرسم بنسبة 0.75 بالضبط على الجوال وسطح المكتب.

### صفحة الفصل
عمود ثابت (غلاف + مشغّل الصوت) يبقى مثبّتاً عند التمرير على الشاشات العريضة، وعمود النص بجانبه. زر رجوع "Back to <اسم الكتاب>" في الأعلى.

### البروفايل
- التبويبات صارت مفتاح تبديل مضغوط بحجم محتواه بدل شريط بعرض الصفحة.
- بطاقات الكتب بنفس صندوق 3:4 مع تدرّج أسود أسفل الصورة ليظهر العنوان والإحصاءات بوضوح.

## 11) ما تم اختباره في هذه الجولة

**19 فحص API للحذف** (بحسابين حقيقيين: مالك وغير مالك) — كلها ناجحة:
```
PASS  1500-char paragraph accepted        PASS  non-owner cannot delete chapter
PASS  2500-char paragraph rejected        PASS  owner deletes chapter
PASS  owner edits paragraph               PASS  non-owner cannot delete book
PASS  non-owner cannot edit paragraph     PASS  owner deletes book
PASS  out-of-range index rejected         PASS  every dependent row removed
PASS  owner removes audio                 PASS  deleted book returns 404
```

**18 فحص واجهة بمتصفح حقيقي** (جوال 375 + سطح مكتب 1280) — كلها ناجحة: عدم وجود تجاوز أفقي، ثبات نسبة الأغلفة، ظهور أزرار الحذف للمالك، نافذة التأكيد، مربّع الإضافة المباشر، أزرار التعديل، وصفر أخطاء JavaScript.

**تدفق كامل بالمتصفح على بيانات مؤقتة:** إضافة فقرة ← تعديلها ← حذفها (الفقرة الأخرى بقيت سليمة) ← حذف فصل من الشبكة ← حذف الكتاب والانتقال إلى البروفايل. كل البيانات المؤقتة حُذفت بعد الاختبار.

**تدقيق ريسبونسف كامل:** 14 صفحة × 4 عروض (320 / 375 / 768 / 1280) = 56 حالة، **صفر تجاوز أفقي وصفر نص مقصوص**.

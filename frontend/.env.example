# Copy to .env.local for development (that file is git-ignored).
# Base URL of the API server.
REACT_APP_API_URL=http://localhost:5000
# Stripe PUBLISHABLE key (pk_test_... / pk_live_...). Never the secret key.
REACT_APP_STRIPE_PUBLISHABLE_KEY=

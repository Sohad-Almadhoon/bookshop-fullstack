import { BsListCheck } from "react-icons/bs";
import { useNotificationModal } from "./store"; // Adjust the import path as needed

const Notifications = () => {
  const { isOpen, toggleModal } = useNotificationModal();

  return (
    <div>
      {/* Notification Button */}
      <button onClick={toggleModal} className="relative z-30 top-3 right-3">
        <BsListCheck className="text-3xl" />
      </button>

      {/* Notification Panel */}
      {isOpen && (
        <div className="w-[400px] border-black border p-2 min-h-96 z-20 absolute top-14 right-10 bg-[#dfd4bf] shadow-lg">
          {/* Arrow */}
          <div className="absolute top-[-10px] right-4 w-4 h-4 bg-[#dfd4bf] rotate-45 border-l border-t border-black" />

          <div className="border-black border p-3 min-h-96">
            <div className="flex items-center justify-between text-3xl">
              <p className="font-bold">Notifications</p>
              <BsListCheck />
            </div>
            <hr className="h-1 my-1 bg-black" />
            {/* Notification Content */}
            <p className="mt-4">You have no new notifications.</p>
          </div>
        </div>
      )}
    </div>
  );
};

export default Notifications;
